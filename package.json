{
  "name": "techkraft-candidate-dashboard",
  "private": true,
  "scripts": {
    "web:dev": "pnpm --filter frontend dev",
    "web:build": "pnpm --filter frontend build",
    "server:start": "cd apps/server && venv/bin/python -m uvicorn app.main:app --reload"
  }
}
